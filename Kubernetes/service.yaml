apiVersion: v1
kind: Service
metadata:
  name: awsconfig-service
spec:
  type: NodePort
  selector:
    app: awsconfig-app
  ports:
  - port: 3000
    targetPort: 3000
# remove nodePort line completely

